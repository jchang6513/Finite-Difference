      SUBROUTINE DIFAB(A,B,DIF)
      IMPLICIT DOUBLE PRECISION (A-H, O-Z)
      COMMON /GET_RERR/ RELERR,ABSERR

      DIF=A-B
      IF(DABS(DIF).LT.MAX(DABS(A),DABS(B))*RELERR) DIF=0.D0
      RETURN
      END
